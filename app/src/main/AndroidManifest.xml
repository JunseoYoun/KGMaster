<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.plzpoint.kgshmaster">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/NoTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Meal.MealActivity" />
        <activity android:name=".Fragment.MealFragment" />
        <activity android:name=".Fragment.ChatFragment" />
        <activity android:name=".Fragment.NotifiFragment" />
        <activity android:name=".Fragment.HomeFragment"></activity>
    </application>

</manifest>